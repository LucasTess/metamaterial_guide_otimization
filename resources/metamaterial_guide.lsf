#metamaterial_guide.lsf
# --- Criação do segmento do guia fragmentado ---

addrect;
set("x min", -Lambda/2);
set("x max", -Lambda/2+Lambda*DC);
set("y", 0);
set('y span', w);
set("z", -0.11e-6 + height/2);
set('z span', height);
set("name", "segment_period");

selectall;
set("material",material);
if(material=="<Object defined dielectric>") {
set("index",index);
}

   

